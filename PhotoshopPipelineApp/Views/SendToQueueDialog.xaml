<Window x:Class="PhotoshopPipelineApp.Views.SendToQueueDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Send to Queue"
        Width="400"
        Height="220"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Select queue" Margin="0,0,0,6"/>
        <ComboBox x:Name="QueueCombo" Grid.Row="1" Margin="0,0,0,12" MinWidth="200" DisplayMemberPath="Name"/>
        <TextBlock Grid.Row="2" Text="Filename (optional)" Margin="0,0,0,6"/>
        <TextBox x:Name="FileNameBox" Grid.Row="3" Margin="0,0,0,16" Padding="6,4" Text="image.png"/>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Height="28" Margin="0,0,8,0" IsCancel="True" Click="CancelButton_Click"/>
            <Button Content="OK" Width="80" Height="28" IsDefault="True" Click="OkButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
